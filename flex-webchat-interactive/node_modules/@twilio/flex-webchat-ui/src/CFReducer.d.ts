import { AppState } from "./state/AppState";
/**
 * Returns Flex WebChat reducer. Combine with custom Redux store.
 *
 * @category Core / State
 * @memberof Store
 * @alias WebchatReducer
 * @function
 * @param {AppState} state current state
 * @param {any} action Redux action
 * @example
 * const reducers = combineReducers({
 *  flex: WebchatReducer,
 *  app: appReducer
 * });
 *
 * @returns {AppState} Flex WebChat state
 * @category Core
 */
declare const CFReducer: import("redux").Reducer<AppState>;
export { CFReducer };
