import { GenericStoreEnhancer } from "redux";
/**
 * Returns the Redux middlewares required for Webchat to work properly.
 * @alias applyWebchatMiddleware
 * @category Core / State
 * @memberof Store
 * @example
 * export const store = createStore(
 *   reducers,
 *   compose(
 *       applyWebchatMiddleware()
 *   )
 * );
 *
 * @returns GenericStoreEnhancer
 */
export declare const applyWebchatMiddleware: () => GenericStoreEnhancer;
