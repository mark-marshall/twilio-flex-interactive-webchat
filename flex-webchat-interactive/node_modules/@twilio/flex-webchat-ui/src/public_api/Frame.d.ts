import { FlexWebChat } from "./FlexWebChat";
import { Config } from "../state/AppConfig";
/**
 * @category Framework
 * @namespace Helpers
 */
/**
 * Create Twilio Flex WebChat. Does not render the instance.
 *
 * @category Framework
 * @memberof Helpers
 * @param {Config} userConfiguration configuration object
 * @returns {Promise<FlexWebChat>}
 * @example
 * Twilio.FlexWebChat.createWebChat(configuration)
 *   .then((webChat) => {
 *     // do something with webChat.manager for example
 *
 *     webChat.init(); // to initialize and render
 *   });
 */
export declare function createWebChat(userConfiguration: Config): Promise<FlexWebChat>;
/**
 * Create and render Flex WebChat instance.
 *
 * @category Framework
 * @memberof Helpers
 * @param {Config} userConfiguration configuration object
 * @returns {Promise<FlexWebChat>}
 * @example
 * Twilio.FlexWebChat.renderWebChat(configuration);
 */
export declare function renderWebChat(userConfiguration: Config): Promise<FlexWebChat>;
