import * as AppConfig from "../state/AppConfig";
import { Manager } from "../core/Manager";
/**
 * @classdesc Flex WebChat client.
 * Create a single instance using {@link FlexWebChat.create create} method.
 * To initialize and render call {@link FlexWebChat.init init} method.
 * @category Core
 */
export declare class FlexWebChat {
    private _manager;
    private _initialized;
    private constructor();
    /**
     * Creates Flex WebChat.
     * In most cases you need only one {@link Manager} instance.
     * This method should be called only once.
     *
     * @param {Config} configuration configuration object
     */
    static create(userConfiguration: AppConfig.Config): Promise<FlexWebChat>;
    /**
     * Manager instance
     * @type {Manager}
     *
     * @readonly
     */
    get manager(): Manager;
    /**
     * Initializes and renders the Web Chat.
     */
    init(): this;
    private createFrameDiv;
}
