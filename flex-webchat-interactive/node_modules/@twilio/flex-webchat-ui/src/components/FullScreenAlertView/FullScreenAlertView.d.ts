import * as React from "react";
interface ButtonDescriptor {
    content: React.ReactChild;
    type: "default" | "primary" | "danger";
    props?: Record<string, any>;
}
export interface FullScreenAlertViewProps {
    showIcon: boolean;
    message?: React.ReactChild;
    primaryDescription?: React.ReactChild;
    secondaryDescription?: React.ReactChild;
    buttons?: Array<ButtonDescriptor>;
    showFlexLogo: boolean;
}
export declare class FullScreenAlertView extends React.PureComponent<FullScreenAlertViewProps> {
    static defaultProps: {
        showIcon: boolean;
        showFlexLogo: boolean;
    };
    render(): JSX.Element;
    renderButtons(buttons: Array<ButtonDescriptor>): JSX.Element[];
}
export {};
