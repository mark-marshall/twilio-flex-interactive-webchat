import * as React from "react";
import { ErrorBoundaryProps } from "./ErrorBoundary.definitions";
interface ErrorBoundaryState {
    error?: Error;
}
export declare class ErrorBoundary extends React.Component<ErrorBoundaryProps, ErrorBoundaryState> {
    state: ErrorBoundaryState;
    /**
     * If we ever need to display the original error, replace the body of setState
     * @param error Original Error
     */
    componentDidCatch(_error: Error): void;
    restartEngagement: () => Promise<void>;
    render(): React.ReactChildren | JSX.Element | React.ReactElement<any, string | ((props: any) => React.ReactElement<any, string | any | (new (props: any) => React.Component<any, any, any>)>) | (new (props: any) => React.Component<any, any, any>)>[];
}
export {};
