import * as React from "react";
import { CSSProps } from "@twilio/flex-ui-core";
import { ThemeProps } from "../theme";
/**
 * @typedef Theme.MainHeaderThemeProps
 * @property {CSSProps} Container - Styles for the header container.
 * @property {CSSProps} Logo - Styles for the header logo.
 */
export interface MainHeaderThemeProps {
    Container: CSSProps;
    Logo: CSSProps;
}
/**
 * Properties of Main header component.
 * @typedef MainHeader.MainHeaderProps
 * @property {string} [titleText="Chat with Us"] - Represent the title of header.
 * @property {string} [imageUrl=undefined] - Represent the url for the logo.
 * @property {boolean} [showTitle=true] - Represents if title to be shown or not.
 * @property {boolean} [showImage=true] - Represents if logo image to be shown or not.
 * @property {Function} [closeCallback] - A callback to trigger when close button is clicked.
 */
export interface MainHeaderProps {
    titleText?: string;
    imageUrl?: string;
    showTitle?: boolean;
    showImage?: boolean;
    closeCallback?: Function;
}
/**
 * @classdesc This is a component for the header.
 * Can be themed with [theme.MainHeader]{@link MainHeader.MainHeaderThemeProps}
 * @component
 * @hideconstructor
 * @param {MainHeader.MainHeaderProps} - Properties to be passed to configure this component.
 * @category Components / Programmable
 */
export declare class MainHeader extends React.PureComponent<MainHeaderProps & ThemeProps> {
    static readonly displayName = "MainHeader";
    /**
     * Dynamic content store
     * @static
     * @type {DynamicContentStore<MainHeader.MainHeaderProps>}
     * @readonly
     */
    static readonly Content: import("@twilio/flex-ui-core").DynamicContentStore;
    /**
     * Default properties
     * @static
     * @type {MainHeader.MainHeaderProps}
     * @readonly
     */
    static readonly defaultProps: MainHeaderProps;
    static updateAll(): void;
    render(): JSX.Element;
}
