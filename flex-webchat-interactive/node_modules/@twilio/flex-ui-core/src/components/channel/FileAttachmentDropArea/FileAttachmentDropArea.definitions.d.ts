import { ChatChannelState } from "../../../state";
import { MessageState } from "../../../state/ChatChannelInputState";
import { CSSProps } from "../../CSSProps";
export interface FileAttachmentDropAreaProps {
    channel: ChatChannelState.ChannelState;
}
export interface FileAttachmentDropAreaImplProps extends FileAttachmentDropAreaProps {
    messageState: MessageState;
}
/**
 * @typedef Theme.FileAttachmentDropThemeProps
 * @property {CSSProps} MainDropArea - Styles for the main attachment drop area
 * @property {CSSProps} Icon - Styles for the icon
 * @property {CSSProps} Title - Styles for the main line of text
 * @property {CSSProps} SubTitle - Styles for the secondary line of text
 */
export interface FileAttachmentDropThemeProps {
    MainDropArea: CSSProps;
    Icon: CSSProps;
    Title: CSSProps;
    SubTitle: CSSProps;
}
