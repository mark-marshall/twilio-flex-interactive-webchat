import * as React from "react";
import { MessageBubbleProps } from "./MessageBubble.definitions";
interface MessageBubbleMediaState {
    hover: boolean;
}
export declare class MessageBubbleMedia extends React.PureComponent<Pick<MessageBubbleProps, "message" | "channelSid"> & {
    disabled: boolean;
}, MessageBubbleMediaState> {
    state: {
        hover: boolean;
    };
    private downloadMedia;
    private matchDownloadMediaPayload;
    private toggleHover;
    private getDataStatus;
    render(): JSX.Element;
}
export {};
