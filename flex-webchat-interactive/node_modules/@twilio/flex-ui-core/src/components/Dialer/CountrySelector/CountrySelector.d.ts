import * as React from "react";
import { CountrySelectorProps, CountrySelectorState } from "./CountrySelector.definition";
export declare class CountrySelector extends React.PureComponent<CountrySelectorProps, CountrySelectorState> {
    menuRef: React.RefObject<any>;
    private _isMounted;
    state: CountrySelectorState;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private getCountries;
    private getFlags;
    private handleCountryChange;
    /**
     * This listener is used to scroll to the matching country when key pressed on list open.
     * This feature is supported on material ui 4 and we can remove this handler when we upgrade.
     */
    private handleKeyPress;
    private renderFlag;
    private renderCountryDetails;
    private renderDefaultMenuItem;
    render(): JSX.Element;
}
