import { RestPaginator } from '../restpaginator';
import { UserDescriptor } from '../userdescriptor';
import { Users } from './users';
import { Network } from '../services/network';
export interface UserDescriptorsServices {
    users: Users;
    network: Network;
}
declare class UserDescriptors {
    private services;
    private url;
    constructor(services: UserDescriptorsServices, url: string);
    getUserDescriptor(identity: string): Promise<UserDescriptor>;
    getChannelUserDescriptors(channelSid: string, args?: any): Promise<RestPaginator<UserDescriptor>>;
}
export { UserDescriptors };
